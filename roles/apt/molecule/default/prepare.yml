---
- name: Prepare
  hosts: all
  become: true

  tasks:
    - name: Install required packages
      ansible.builtin.apt:
        name:
          - python3-debian
        state: present
        update_cache: true
