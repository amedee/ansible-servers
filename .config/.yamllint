---
extends: default

rules:

  comments:
    level: error

  # 120 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 120
    level: warning

  truthy:
    level: error
